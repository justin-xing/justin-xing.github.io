import classes from "./Blog.module.css";

const Blog = () => {
  return (
    <div className={classes.blogWrapper}>
      Page removed temporarily as Twitter changed its embed API.
    </div>
  );
};
export default Blog;
